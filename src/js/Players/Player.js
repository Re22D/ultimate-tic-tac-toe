export class Player{
    /**
     * 
     * @param {string} name players' name
     * @param {char} sign X/O
     * @param {string} type player type
     */
    constructor(name, sign, type){
        this.name = name;
        this.sign = sign;
        this.type = type;
    }

}
